# Model A Reports

## Folders

- `metrics/`
  JSON metrics for baseline, RF_abs, RF_pct, and current v2 tier-split model.
- `analysis/`
  CSV analysis outputs used for validation and reporting.
- `charts/`
  Current v2 charts.

## Archive note

Older single-model RF assets were moved into:

- `analysis/legacy_rf_pct/`
- `charts/legacy_rf_pct/`

This keeps the current v2 outputs easier to identify.
